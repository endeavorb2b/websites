$ const { req } = out.global;
$ const { identityX } = req;

<identity-x-context|{ user, isEnabled }|>
  $ const props = {
    activeUser: user,
    endpoints: identityX.config.getEndpoints(),
  };
  <if(isEnabled)>
    <cms-browser-component name="IdentityXLogin" props=props />
  </if>
</identity-x-context>
