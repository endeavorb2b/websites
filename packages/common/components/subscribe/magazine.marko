import { getAsObject } from '@base-cms/object-path';

$ const publication = getAsObject(input, 'publication');

<h1>Subscribe to ${publication.name}</h1>
<iframe src=publication.subscribeUrl width="100%" height="700px" frameborder=0 scrolling="auto" />
