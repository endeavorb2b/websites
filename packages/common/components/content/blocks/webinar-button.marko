import { get } from '@base-cms/object-path';
<endeavor-link
  class="btn btn-primary btn-lg"
  href=`${get(input, 'content.linkUrl')}`
  target=input.target
  title=input.title
>
  ${input.label}
</endeavor-link>
