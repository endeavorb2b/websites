import { asArray, asObject } from '@base-cms/utils';

$ const block = 'content-page-title';

<div class=`${block} card`>
  <if(input.header)>
    <h1 class=`${block}__header card-header`>
      ${input.header}
    </h1>
  </if>
  <div class=`${block}__body card-body`>
    ${input.body} &nbsp;
  </div>
</div>
