import { get } from '@base-cms/object-path';
<endeavor-link
  class="btn btn-primary btn-lg"
  href=`${get(input, 'content.fileSrc')}`
  target=input.target
  title=input.title
>
  ${input.label}
</endeavor-link>
